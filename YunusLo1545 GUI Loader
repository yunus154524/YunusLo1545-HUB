local ScreenGui = Instance.new("ScreenGui")
local Frame = Instance.new("Frame")
local UICorner = Instance.new("UICorner")
local Title = Instance.new("TextLabel")
local StartButton = Instance.new("TextButton")
local TweenService = game:GetService("TweenService")

ScreenGui.Parent = game.CoreGui
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

Frame.Parent = ScreenGui
Frame.AnchorPoint = Vector2.new(0.5, 0.5)
Frame.Position = UDim2.new(0.5, 0, 0.5, 0)
Frame.Size = UDim2.new(0, 450, 0, 220)
Frame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
Frame.BackgroundTransparency = 0.1
Frame.Active = true
Frame.Draggable = true

UICorner.Parent = Frame

Title.Parent = Frame
Title.Size = UDim2.new(1, 0, 0, 60)
Title.BackgroundTransparency = 1
Title.Text = "🔥 YunusLo1545 GUI Loader"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.Font = Enum.Font.GothamBold
Title.TextSize = 24

StartButton.Parent = Frame
StartButton.AnchorPoint = Vector2.new(0.5, 0.5)
StartButton.Position = UDim2.new(0.5, 0, 0.6, 0)
StartButton.Size = UDim2.new(0, 300, 0, 60)
StartButton.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
StartButton.Text = "🚀 Launch Menu"
StartButton.TextColor3 = Color3.fromRGB(255, 255, 255)
StartButton.Font = Enum.Font.GothamBold
StartButton.TextSize = 20

local corner2 = Instance.new("UICorner")
corner2.Parent = StartButton

StartButton.MouseEnter:Connect(function()
    TweenService:Create(StartButton, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(60, 60, 60)}):Play()
end)

StartButton.MouseLeave:Connect(function()
    TweenService:Create(StartButton, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(40, 40, 40)}):Play()
end)

StartButton.MouseButton1Click:Connect(function()
    StartButton.Text = "⏳ Loading..."
    pcall(function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/yunus154524/YunusLo1545-HUB/refs/heads/main/YunusLo1545%20HUB"))()
    end)
    TweenService:Create(Frame, TweenInfo.new(0.5), {Size = UDim2.new(0,0,0,0)}):Play()
    wait(0.5)
    ScreenGui:Destroy()
end)

-- Opening animation
Frame.Size = UDim2.new(0, 0, 0, 0)
TweenService:Create(Frame, TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Size = UDim2.new(0, 450, 0, 220)}):Play()
